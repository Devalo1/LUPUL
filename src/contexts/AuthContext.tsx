/**
 * CENTRAL AUTHENTICATION EXPORT POINT
 * This file serves as the main entry point for authentication functionality.
 * Import from this file in your components, not directly from AuthProvider.
 */
import { AuthProvider, useAuth } from './AuthProvider';

export { AuthProvider, useAuth };
