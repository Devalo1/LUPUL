// This file provides exports for backward compatibility
import { AuthContext } from "./AuthContextDef";
import { useAuth } from "./useAuth";

// Export the hooks and context
export { useAuth, AuthContext };
